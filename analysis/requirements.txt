# The whole dependency list. The core crate's austerity does not extend here --
# this layer is allowed pandas and matplotlib because reading a CSV and drawing
# a chart are exactly what they are for -- but it stops at those two.
pandas>=2.0
matplotlib>=3.7
